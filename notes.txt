*************************************************************** EPISODE 1 *****************************************************

Q. What is Emmet?
A. It is a popular web development toolkit and code plugin. It allows developers to write and generate HTML and CSS code at lightning speed using abbreviations.

Q. Difference between a Library and Framework?
A. Library is a collections of prewritten code snippets that can be used and reused to perform certain tasks. A particular JavaScript library code can be plugged into 
application code which leads to faster development and fewer vulnerabilities to have errors. eg react, jQuery
        Framework provides a basic foundation or structure for a website or an application. eg angular

        A key difference between the two is Inversion of control. When using a library, the control remains with the developer who tells the application when to call 
library functions. When using a framework, the control is reversed, which means that the framework tells the developer where code needs to be provided and calls it as 
it requires.

Q. What is React?
A. React is split into two main parts that you must load into your project:
1. React Core: This handles the logic of creating components, managing state, and building the "Virtual DOM."
2. ReactDOM: This is the glue that takes that Virtual DOM and actually renders it into the real browser DOM.

Q. Why is React known as React?
A. React is a JavaScript Library. The name ‘React’ was chosen because the library was designed to allow developers to react to changes in state and data within an 
application, and to update the user interface in a declarative and efficient manner.

Q. What is diference between React and ReactDOM
A. React: React is a javascript library, designed for building better user interfaces.
React-DOM: React-DOM is a complimentary library to React which glues React to the browser DOM
Whenever we use component, classes, elements, etc. We’re using React and whenever we use methods like render() or findDOMNode() we’re using React-DOM.
Earlier the ReactDOM was part of React package, but the split happened because React has nothing to do with browsers. That’s why we’re seeing tools and frameworks like
React-Native, React-Three being developed. These tools and frameworks don’t use React-DOM, but they do in fact use the idea behind React.

Q. What is difference between react.development.js and react.production.js files via CDN?
A. react.development.js -> 
    a. Ideal for development environments where debugging and code clarity are priorities.
    b. Unminified Code: Maintains readable code structure for easier debugging and understanding.
    c. Source Maps: Generates files that link back to the original source code, making debugging easier.
   react.production.js ->
    a. Optimized for real-world deployment, prioritizing performance and file size
    b. Minified Code: Significantly smaller file size by removing unnecessary whitespace and shortening variable names, leading to faster loading times.
    c. No Source Maps: Excludes source maps for reduced file size and potential security reasons.


*************************************************************** EPISODE 2 *****************************************************

Q. What is `NPM`?
A. npm doesnt stand for node package manager but does the job of managing the packages. npm allow the developers to install and manage JavaScript packages and project dependencies.

Q. What is `Parcel/Webpack`? Why do we need it?
A. It is a bundler which is used to bundle multiple JS/CSS/assets into optimized output files. They also handle code splitting, tree shaking, caching, and development servers.

Q. What is `.parcel-cache`
A. .parcel-cache stores Parcel's build cache. It allows Parcel to reuse previous build information and perform faster incremental rebuilds.

Q. What is `npx` ?
A. npx executes packages. It can run locally installed packages directly from node_modules.

Q. What is difference between `dependencies` vs `devDependencies`?
A. dependencies and devDependencies are managed in package.json file. dependencies include the dependencies that are required for production eg react, react-dom but 
   devDependencies include the dependencies that are only needed for development purpose eg jest, parcel.

Q. What is Hot Module Replacement?
A. Hot Module Replacement updates only the modified module in the browser without performing a full page reload, preserving application state and improving development experience.

Q. List down your favourite 5 superpowers of Parcel and describe any 3 of them.
A. 1. Hot Reloading
        - As you make changes to your code, Parcel automatically rebuilds the changed files and updates your app in the browser. By default, Parcel fully reloads the page, but in some 
          cases it may perform Hot Module Replacement (HMR). HMR improves the development experience by updating modules in the browser at runtime without needing a whole page refresh. 
          This means that application state can be retained as you change small things in your code.

        - HMR works by replacing the code for a module, and then re-evaluating it and along with all of its parents.
   2. Diagnostics
        - If you make an error in your code or configuration, Parcel displays beautiful diagnostics in your terminal and in the browser.
   3. Tree Shaking
        - It statically analyzes the imports and exports of each module, and removes everything that isn't used.
        - In production builds, Parcel concatenates modules into a single scope when possible, rather than wrapping each module in a separate function. This is called “scope hoisting”.
          Parcel’s implementation of scope hoisting works by analyzing each module independently and in parallel, and at the end concatenating them together. In order to make 
          concatenation into a single scope safe, the top-level variables of each module are renamed to ensure they are unique. In addition, imported variables are renamed to match the 
          exported variable names from the resolved module. Finally, any unused exports are removed.
          eg before tree shaking
          (a) index.js
                import {add} from './math';

                console.log(add(2, 3));

          (b) math.js
                export function add(a, b) {
                return a + b;
                }

                export function square(a) {
                return a * a;
                }
          After Tree Shaking - the files will be combined in single module and variables names would be changed
                function $fa6943ce8a6b29$add(a, b) {
                return a + b;
                }

                console.log($fa6943ce8a6b29$add(2, 3));

   4. Minification
        - Minification reduces the file size of your output bundles by removing whitespace, renaming variables to shorter names,
   5. Content Hashing
   6. Caching
        - Parcel caches everything it builds to disk. If you restart the dev server, Parcel will only rebuild files that have changed since the last time it ran. Parcel automatically 
        tracks all of the files, configuration, plugins, and dev dependencies that are involved in your build, and granularly invalidates the cache when something changes. For example,
        if you change a configuration file, all of the source files that rely on that configuration will be rebuilt.

        - Parcel caches everything it builds to disk. If you restart the dev server, Parcel will only rebuild files that have changed since the last time it ran. Parcel automatically 
        tracks all of the files, configuration, plugins, and dev dependencies that are involved in your build, and granularly invalidates the cache when something changes. For example, 
        you change a configuration file, all of the source files that rely on that configuration will be rebuilt.
   7. Dev server
        - Parcel includes a development server which is run on https://localhost:1234
   8. Differential Bundling

Q. What is `.gitignore`? What should we add and not add into it?
A. .gitignore specifies files and directories that Git should not track or commit. We should add all the files in .gitignore that could be regenerated
   by other files like .parcel-cahce, dist, node_modules

Q. What is the difference between `package.json` and `package-lock.json`
A. (a) package.json 
   - maintains approximate version of the installed dependencies through ^/~ . 
   - maintains the installed devDependencies and dependencies. 
   - contains metadata and scripts

   (b) package-lock.json
   - maintains the exact version of installed dependencies
   - maintains the version of installed transitive dependencies

Q. Why should I not modify `package-lock.json`?
A. package-lock.json ensures consistent dependency versions across environments. Manual modification can lead to inconsistent installs and dependency resolution issues.

Q. What is `node_modules` ? Is it a good idea to push that on git?
A. node_modules folder is generated when we run 'npm install' because it contains the code of all the installed packages/dependencies. One should not push it to git because it consumes
   a lot of space, also it can be regenerated through package.json and package-lock.json(reproducible pieces are not advised to push into git). It contains both direct and transitive 
   dependencies.

Q. What is the `dist` folder?
A. dist contains the production build artifacts generated by the bundler. These files are optimized, bundled, minified, and ready for deployment.

Q. What is `browserlists`
A. It defines on which browsers and their versions, our web page will be compatible with!

Q. Read about: ^ - caret and ~ - tilda
A. caret symbol allows minor and patch updates within the same major version whereas tilda allows patch updates.

Q. Read about dif bundlers: vite, webpack, parcel
A. 

Q. What is transpilation?
A. Transpilation (short for "source-to-source compilation") is the process of converting human-readable source code from one high-level programming language into another high-level 
   language. Unlike traditional compiling, which turns code into machine-readable binary (0s and 1s), transpiling keeps the code in a human-readable, high-level format.

                This is used for CROSS ENVIRONMENT COMPATIBILTY: Developers write modern code (like ES6+ JavaScript, TypeScript, or JSX), but browsers cannot read these newer syntaxes 
    natively. Transpiling converts this modern code into older, widely supported JavaScript (like ES5).



*************************************************************** EPISODE 3 *****************************************************

Q. What is 'type' in script tag?
A. The type attribute gives the language of the script or format of the data. Permissible values for type attribute are:
        1. text/javascript or omitted
        - Interpreted as classic Javascript script.
        - You do not need to include type="text/javascript" in modern HTML5. The browser defaults to JavaScript automatically.

        2. module
        - Instructs the browser to treat the code as an ES (ECMAScript)
        - automaticcaly loads as defer attribute.
        - allows to have imports/exports at top of script.
        
        3. importmap

        4. speculationrules

        5. text/template or application/json
        - If the browser does not recognize the value as a scripting language, it completely ignores the script and will not execute it. This is highly useful for 
        embedding data directly inside HTML that your application's JavaScript can read later using the DOM


*************************************************************** EPISODE 4 *****************************************************
Q. What is ECMAScript?
A. ECMAScript is language specification and JS is its implementation. A specification is a formal, written document that defines the rules, syntax, and behavior of a programming language. It doesn't contain any code that a computer can run; it is purely a guide for humans.
	- It answers questions like:
		1. What keywords are allowed? (e.g., if, for, function)
		2. What should happen when you add a number to a string?
		3. How should the language handle errors?

Q. Is JSX mandatory for React?
A. No! One can build components using React.createElement too. React.createELement is pure JS. Each JSX element is just syntactic sugar for calling React.createElement(component, props, ...children)

Q. Is ES6 mandatory for React?
A. No. If one doesn’t use ES6 yet, one may use the create-react-class.

Q. {TitleComponent} vs {<TitleComponent/>} vs {<TitleComponent></TitleComponent>} in JSX
A.  1.{TitleComponent} (The Reference)
	- This passes the actual JavaScript function itself as an expression. It does not execute or render the component.
	- One can only use it when passing a component as a prop to another component (e.g., <Route component={TitleComponent} />
	
	2. {<TitleComponent />}
	- This executes the component and creates a React element. It is the standard way to render a component that has no children.
	- Syntactic sugar for React.createElement(TitleComponent, null)
	- It calls the TitleComponent function, executes its logic, and renders whatever JSX is inside it.
	
	3. {<TitleComponent></TitleComponent>}
	- This behaves exactly identical to {<TitleComponent />} under the hood, but it allows you to pass nested content (children) inside it.
	- Syntactic sugar for React.createElement(TitleComponent, null, ...children)
	- written in this way when one plan to put something between the tags, which will be passed to TitleComponent as props.children.
		Example: {<TitleComponent>Click Me</TitleComponent>}


Q. What is Virtual DOM? What is Reconciliation in React?
A. The Real DOM (Document Object Model) is the browser's internal tree representation of your webpage. Updating the Real DOM is slow because every time a HTML element changes, the browser has to recalculate the CSS, layout the page, and repaint the screen.
   The Virtual DOM is a lightweight, conceptual copy of the Real DOM kept in the computer's memory.
   It is made up of plain JavaScript objects.
   Because it's just regular JavaScript and doesn't touch the screen, creating or changing nodes in the VDOM is incredibly fast.
   
   Reconciliation is the process or "syncing algorithm" React uses to update the browser OR The algorithm React uses to diff one tree with another to determine which parts need to be changed. Instead of rendering everything from scratch when something changes, React uses a highly optimized strategy to update only what is absolutely necessary.
   Here is the step-by-step process of how it works:
   1. The Render Phase: When a component's state or props change, React creates a brand-new Virtual DOM tree representing how the UI should look.
   2. The Diffing Phase: React takes this new VDOM tree and compares it with the old VDOM tree (the one created right before the update). React uses a "diffing algorithm" to figure out exactly what changed.
   3. The Commit Phase: Once React knows the exact differences (e.g., “Only the text inside the <h1> changed, and a new <li> was added”), it surgically updates only those specific parts in the Real DOM.
   
   The Two Golden Rules of the Diffing Algorithm:
   Comparing two trees normally takes O(n^3) mathematical operations, which would be way too slow for a web browser. React cuts this down to O(n) by making two assumptions during reconciliation:
    - Different Element Types = Brand New Trees: If an element changes from a <div> to a <span>, React won't bother comparing their children. It will completely tear down the old tree and build the new one from scratch.
	- The Power of Keys: When rendering lists, React uses the key prop to keep track of elements across renders. If you reorder a list, React uses the keys to shuffle the existing Real DOM elements around rather than destroying and recreating them.

  ALTER defination of Reconciliation:
  Reconciliation is the algorithm behind what is popularly understood as the "virtual DOM." A high-level description goes something like this: when you render a React application, a tree of nodes that 
  describes the app is generated and saved in memory. This tree is then flushed to the rendering environment — for example, in the case of a browser application, it's translated to a set of DOM operations. 
  When the app is updated (usually via setState), a new tree is generated. The new tree is diffed with the previous tree to compute which operations are needed to update the rendered app.

Q. Why we need keys in React? When do we need keys in React?
A. By default, when recursing on the children of a DOM node, React just iterates over both lists of children at the same time and generates a mutation whenever there’s a difference.
   For example, when adding an element at the end of the children, converting between these two trees works well:
    <ul>
	  <li>first</li>
	  <li>second</li>
	</ul>

	<ul>
	  <li>first</li>
	  <li>second</li>
	  <li>third</li>
	</ul>
	React will match the two <li>first</li> trees, match the two <li>second</li> trees, and then insert the <li>third</li> tree.
	
	If you implement it naively, inserting an element at the beginning has worse performance. For example, converting between these two trees works poorly:
	<ul>
	  <li>Duke</li>
	  <li>Villanova</li>
	</ul>

	<ul>
	  <li>Connecticut</li>
	  <li>Duke</li>
	  <li>Villanova</li>
	</ul>
	React will mutate every child instead of realizing it can keep the <li>Duke</li> and <li>Villanova</li> subtrees intact. This inefficiency can be a problem.
	
	In order to solve this issue, React supports a key attribute. When children have keys, React uses the key to match children in the original tree with children in the subsequent tree
	<ul>
	  <li key="2015">Duke</li>
	  <li key="2016">Villanova</li>
	</ul>

	<ul>
	  <li key="2014">Connecticut</li>
	  <li key="2015">Duke</li>
	  <li key="2016">Villanova</li>
	</ul>
    Now React knows that the element with key '2014' is the new one, and the elements with the keys '2015' and '2016' have just moved

Q. Can we use index as keys in React?
A. Yes but is not recommended. item’s index in the array as a key can work well if the items are never reordered, but reorders will be slow.
   Reorders can also cause issues with component state when indexes are used as keys. Component instances are updated and reused based on their key. If the key is an index, moving an item changes it. As a result, component state for things like uncontrolled inputs can get mixed up and updated in unexpected ways.
   a key is the only thing React uses to identify DOM elements. What happens if you push an item to the list or remove something in the middle? If the key is same as before React assumes that the DOM element represents the same component as before. But that is no longer true.
   e.g. https://jsbin.com/wohima/edit?output -> try this!
   
Q. What is <React.Fragment></React.Fragment> and <></> ?
A. 

Q. What is React Fiber?
A. - Its headline feature is incremental rendering: the ability to split rendering work into chunks and spread it out over multiple frames. Other key features include the ability to pause, abort, or reuse work
     as new updates come in; the ability to assign priority to different types of updates;
   - Primary goal of Fiber is to enable React to take advantage of scheduling. Specifically, we need to be able to
      - pause work and come back to it later.
      - assign priority to different types of work.
      - reuse previously completed work.
      - abort work if it's no longer needed.

   Render vs rendering
   The reconciler does the work of computing which parts of a tree have changed; the renderer then uses that information to actually update the rendered app.This separation means that React DOM and React 
   Native can use their own renderers while sharing the same reconciler, provided by React core.Fiber reimplements the reconciler.

   Scheduling in Fiber:
   - the process of determining when work(any computations that must be performed) should be performed.
   - In its current implementation React walks the tree recursively and calls render functions of the whole updated tree during a single tick. However in the future it might start delaying some updates to avoid dropping frames.
   - This is a common theme in React design. Some popular libraries implement the "push" approach where computations are performed when the new data is available. React, however, sticks to the "pull" approach where computations can be delayed until necessary.
   - React is not a generic data processing library. It is a library for building user interfaces. We think that it is uniquely positioned in an app to know which computations are relevant right now and which are not.
   - If something is offscreen, we can delay any logic related to it. If data is arriving faster than the frame rate, we can coalesce and batch updates. We can prioritize work coming from user interactions 
     (such as an animation caused by a button click) over less important background work (such as rendering new content just loaded from the network) to avoid dropping frames.
   - React doesn't currently take advantage of scheduling in a significant way; an update results in the entire subtree being re-rendered immediately. Overhauling React's core algorithm to take advantage of scheduling is the driving idea behind Fiber
   

*************************************************************** EPISODE 5 *****************************************************
Q. What is the difference between Named Export, Default export ?
A.  1. Named import/export
		- this is used when we want to export multiple functions/statements present in a single file
		- syntax: export const logo_url = ".......";
				  export function Header = () => {
													console.log("Demo function");
											}
											
				  import {logo_url, Header} from "......";
				  
				  import * as utils from "......";
				  console.log(utils.Header};
				  console.log(utils.logo_url);
				  
	2. Default export
		- this is used when we want to export a single function/component/statement from a file
		- syntax: function Header = () => {
											console.log("demo function");
										  } 
				  export default Header;
				  
				  import Header from ".....";



Q. What is the importance of config.js file?
A. A config.js file's primary purpose is to keep variable, environment-specific, or global data separate from your actual application logic.Instead of scattering hard-coded values across dozens of different code files, you put them all in 
   config.js and import them wherever they are needed.

Q. What are React Hooks?
A. Hooks are special functions in React that let you use state variable. Rules imposed on hooks are:
		- Only call Hooks at the top level. Don’t call Hooks inside loops, conditions, or nested functions.
		- Only call Hooks from React function components. Don’t call Hooks from regular JavaScript functions. 

Q. Why do we need a useState Hook?
A. - useState is a Hook. We call it inside a function component to add some local state to it.React will preserve this state between re-renders. 
   - useState returns a pair: the current state value and a function that lets you update it. The only argument to useState is the initial state


*************************************************************** EPISODE 6 *****************************************************

Q. Why do we need a useEffect Hook?
A. useEffect is a React Hook that lets you perform side effects in function components. Side effects can include:
      1. Fetching data from an API
      2. Subscribing to a service
      3. Manually updating the DOM
      4. Setting up timers or intervals

   The useEffect hook takes two arguments:
      - A function (your side effect).
      - An optional dependency array.

   Dependency Array:
   1. Run on Every Render (No Dependency Array):
         useEffect(() => {
         console.log('Runs after every render');
         });
   2. Run Only Once (Empty Dependency Array):
         useEffect(() => {
         console.log('Runs only once (like componentDidMount)');
         }, []);
   3. Run When State/Props Change:
         useEffect(() => {
         console.log('Runs when count changes');
         }, [count]);

   Cleaning Up Effects: Sometimes, side effects need cleanup — like removing event listeners or canceling API calls. useEffect lets you return a cleanup function to handle this.
         useEffect(() => {
         const timer = setInterval(() => {
            console.log('Interval running');
         }, 1000);
         return () => {
            clearInterval(timer);  // Cleanup on unmount
            console.log('Interval cleared');
         };
         }, []);

Q. What is the difference between JS expression and JS statement
A. Expression:
      An expression is any valid unit of code that resolves to a single value. Wherever JavaScript expects a value, you can put an expression
      eg: 
         Basic Values: 5, "Hello World", true
         Arithmetic Operations: 2 + 3 (resolves to 5)
         Variable References: myVariable (resolves to whatever is stored inside)
         Function Calls: calculateTotal() (resolves to the returned value)
         Ternary Operators: isLoggedIn ? 'Welcome' : 'Login'

   Statement: 
      A statement is an instruction to the JavaScript engine to perform a task. 
      eg:
         Variable Declarations: let x = 10;
         Conditional Structures: if (x > 5) { ... }
         Loops: for (let i = 0; i < 10; i++) { ... } or while (condition) { ... }
         Return/Jump Commands: return; or break;

Q. What is Conditional Rendering, explain with a code example
A. To display different things depending on different conditions. One can conditionally render JSX using JavaScript syntax like if statements, &&, and ? : operators

Q. What is CORS?
A. stands for cross origin resource sharing which is implemented by browser that extends the Same-Origin Policy. It restricts to access the data when requested by a 
   different origin(https/http + host + port) combination. 
   By default browsers block the requests from different origin. Using CORS, servers can mention which cross origins can access the data by using Access-Control-Allow-Origin
   in their response headers. The browser checks these headers before allowing the client-side JavaScript to access the response.

   FYI: CORS does not stop the request from reaching the server. It stops JavaScript from reading the response. So when a client makes a request, it is sent to server
   and server does all its job and then sends the response headers. THe browser then matches the Access-Control-Allow-Origin header present in response headers and allow
   to access the data(or denies the request).


*************************************************************** EPISODE 7 *****************************************************

Q. What are various ways to add images into App? Explain with code examples
A.  1. Importing images using ES6 Modules

		import React from 'react';
		import myImage from './my_image.jpg';
		function App() {
		 return (
		 <div>
		 <img src={myImage} alt="My Image" />
		 </div>
		 );
		}
		export default App;
		
	2. Using public folder
			This method is useful for handling large image assets or for dynamic image URLs. Place your image in the public directory. Then, reference it in your code:
	
		// public/my_image.jpg       ------>   place in folder
		
		import React from 'react';
			function App() {
			 return (
			 <div>
			 <img src={process.env.PUBLIC_URL + '/my_image.jp
			g'} alt="My Image" />
			 </div>
			 );
			}
		export default App;
	
	3. Loading images from a remote source
		We can load images from a remote source, such as an external URL or a backend API, by specifying the image URL directly in img tag.
	
		import React from 'react';
			function App() {
			 const imageUrl = 'https://example.com/my_image.jpg';
			 return (
			 <div>
			 <img src={imageUrl} alt="My Image" />
			 </div>
			 );
		}
		export default App;
		
	4. Using image assets within CSS
		We can also use images as background images or in other CSS styling
		
		image-container {
			 background-image: url('/my_image.jpg');
			 width: 300px;
			 height: 200px;
		}
		
		Then, apply the CSS class to your JSX:
		
		import React from 'react';
		import './styles.css';
		function App() {
			 return (
			 <div className="image-container">
			 {/* Content goes here */}
			 </div>
			 );
		}
		export default App;

Q. What would happen if we do console.log(useState())?
A. It depends on where you call it.
   If you call it outside of a React component, it will throw a runtime error because hooks can only be called inside functional components.
   If you call it inside a component, it returns an array: [undefined, dispatchAction]. The first element is undefined because no initial state was passed to the hook, and the second element is the state updater function.
   The synatx we commonly use with useState() as const [value, setValue] = useState(0) is the array destructuring syntax.

Q. What is SPA?
A. An SPA, or Single Page Application, is a web application that loads a single HTML page and dynamically updates that page as the user interacts with it.
   Instead of the browser requesting a brand-new HTML page from the server on every navigation, client-side routing intercepts the URL change and swaps out JavaScript components instantly  and avoids full page refresh.
   The trade-offs are a slower initial load time—because the browser has to download a larger JavaScript bundle upfront.

*************************************************************** EPISODE 8 *****************************************************

Q. Lifecycle methods
A. Constructor - we recieve props here, state variables are also defined here.
   Render - returns JSX
   ComponentDidMount - for one time initializations and making API calls
   ComponentDidUpdate - to call something that needs to be invoked when any state variable is changed
   COmponentWillUnmount - cleanup (i.e. cleaning setInterval)

Q. Why we use super(props) in constructor of class based components?
A. This is because super(props) is used to invoke the constructor of the parent class, ensuring that the parent class's initialization is performed before the child class's constructor
   code is executed. It is essential to maintain the inheritance chain correctly.
   By calling super(props), we also make the props available for the parent class's constructor to work with.

Q. Why can't we have the callback function of useEffect async?
A.  useEffect itself cannot directly accept an async callback function. This is because useEffect expects its callback function to return either nothing (i.e., undefined) or a cleanup
   function. An async function returns a Promise, and it doesn't fit well with this expected behavior.
   With async functions, we might not have fine-grained control over the execution order of the asynchronous code and the cleanup code.
   React relies on the returned cleanup function to handle cleanup when the component is unmounted or when the dependencies specified in the useEffect dependency array change. If you return a Promise, React doesn't know when or
   how to handle cleanup.
   One can use async in below way:
   
   useEffect(() => {
      const fetchData = async () => {
         try {
            // Perform asynchronous operations
            const result = await someAsyncOperation();
            // Update the state with the result
            setState(result);
         } catch (error) {
            // Handle errors
            console.error(error);
         }
      };

      fetchData(); // Call the async function
      return () => {
         // Cleanup code, if necessary
         // This function will be called when the component unmounts or when dependencies change
      };
   }, [/* dependency array */]);

Q. How do you create Nested Routes react-router-dom cofiguration
A. 

Q. Read abt createHashRouter, createMemoryRouter from React Router docs.
A.

*************************************************************** EPISODE 9 *****************************************************

Q. Advantages and disadvantages of using this code splitting pattern?
A. Advantages:
   - Faster Initial Load Time
   - Improved Performance
   - Optimized Resource Usage 
   - Enhanced Caching: Smaller bundles can benefit from browser caching. Since they are less likely to change frequently, browsers can cache them, resulting in faster subsequent visits for returning users
   - Simpler Maintenance 
   - Better Mobile Performance

   Disadvantages
   - Complex Configuration : Setting up code splitting and configuring it correctly can be complex, especially in large applications
   - Initial Loading Delay : though it can be compensated with <Suspense>
   - Testing Complexity

Q. When and why do we need lazy() ?
A. We use lazy for: 
   - Code Splitting and Reducing Initial Bundle Size
   - Improved Performance
   - Faster Initial Load
   - Better User Experience
   - Optimizing Mobile Performance
   we need to use lazy() when we want to optimize the performance and user experience of our React application by reducing the initial bundle size and deferring the loading of components until they are needed.

*************************************************************** EPISODE 10 *****************************************************
Q. Why do we have .postcssrc file?
A. 

Q. In tailwind.config.js , what does all the keys mean (content, theme, extend, plugins)?
A. 

Q. Advantages of tailwind css.
A. 


